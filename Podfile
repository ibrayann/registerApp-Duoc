platform :ios, '12.0'
use_frameworks!

target 'registerApp' do
  # Agrega las dependencias que desees aquí
  pod 'Alamofire', '~> 5.4.3'
  pod 'SwiftyJSON', '~> 5.0.0'
  project 'registerApp.xcodeproj'
  # Ejemplo de dependencias, asegúrate de agregar las que necesitas
end
